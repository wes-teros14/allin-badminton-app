---
phase: 17-manual-batch-allocation-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - badminton-v2/src/components/ManualBatchPickerDialog.tsx
autonomous: true
requirements:
  - MAN-01
  - MAN-02
  - MAN-03
must_haves:
  truths:
    - "Manual batch selection happens in a picker dialog, not inline on the finance card"
    - "Brand search filters live as the QM types using a case-insensitive contains match"
    - "Empty-search suggestions default to the cheapest available batches first"
    - "Each result row shows full inventory-style details before selection"
    - "Each result row supports one-tap add"
  artifacts:
    - path: "badminton-v2/src/components/ManualBatchPickerDialog.tsx"
      provides: "dialog-based searchable manual batch picker"
      contains: "Add"
---

<objective>
Build the standalone picker dialog component for manual batch search and one-tap selection.

Purpose: The user explicitly chose a dialog-based picker with live brand filtering and full inventory-style detail rows. Building it as a dedicated component keeps the search-heavy interaction isolated from the finance page orchestration.
Output: A reusable picker dialog component renders cheapest-first suggestions, live brand filtering, inventory-style details, and one-tap add actions.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-manual-batch-allocation-ui/17-CONTEXT.md
@.planning/phases/17-manual-batch-allocation-ui/17-RESEARCH.md
@.planning/phases/17-manual-batch-allocation-ui/17-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManualBatchPickerDialog with live brand search</name>
  <files>badminton-v2/src/components/ManualBatchPickerDialog.tsx</files>
  <read_first>
    - badminton-v2/src/components/ui/dialog.tsx
    - badminton-v2/src/views/InventoryView.tsx
    - badminton-v2/src/components/RosterPanel.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Create a dedicated dialog component for manual batch picking.

Recommended contract:
- controlled `open` / `onOpenChange`
- list of available batch options from `useSessionFinance`
- callback for one-tap add
- optional list of already selected batch ids so the dialog can disable or hide duplicates without implementing Phase 18 validation logic

Inside the dialog:
- render a brand search input
- filter live with a case-insensitive `includes()` match
- when search is empty, show cheapest-first default suggestions
- keep the component self-contained and free of finance-page save logic
  </action>
  <done>
    - Dialog component exists with live search behavior
    - Empty-search suggestions follow cheapest-first ordering
  </done>
</task>

<task type="auto">
  <name>Task 2: Render full inventory-style result rows with one-tap add</name>
  <files>badminton-v2/src/components/ManualBatchPickerDialog.tsx</files>
  <read_first>
    - badminton-v2/src/components/ManualBatchPickerDialog.tsx
    - badminton-v2/src/views/InventoryView.tsx
    - .planning/phases/17-manual-batch-allocation-ui/17-CONTEXT.md
  </read_first>
  <action>
Render each search result so it mirrors the inventory detail level the user selected:
- tube ID
- brand
- shuttles remaining
- cost per tube
- notes when present

Each result row should expose a direct `Add` action that calls the supplied callback immediately.

Do not integrate this component into `FinanceDetailView` yet. Keep this plan focused on the picker itself.
  </action>
  <done>
    - Result rows match the inventory detail contract
    - One-tap add behavior is implemented at the component boundary
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run build` exits 0
- `npm.cmd run lint` exits 0
- `grep -n "includes" badminton-v2/src/components/ManualBatchPickerDialog.tsx` returns the live brand filter logic
- `grep -n "Add" badminton-v2/src/components/ManualBatchPickerDialog.tsx` returns one-tap add UI lines
</verification>

<success_criteria>
- Manual picker exists as a dialog component
- Search is live and brand-based
- Results show full inventory-style details with one-tap add
</success_criteria>

<output>
After completion, create `.planning/phases/17-manual-batch-allocation-ui/17-02-SUMMARY.md`
</output>
