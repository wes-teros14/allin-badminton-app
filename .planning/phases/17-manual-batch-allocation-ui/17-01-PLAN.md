---
phase: 17-manual-batch-allocation-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - badminton-v2/src/hooks/useSessionFinance.ts
  - badminton-v2/src/__tests__/useSessionFinance.test.ts
autonomous: true
requirements:
  - MAN-02
  - MAN-03
  - MAN-04
  - MAN-05
  - MAN-06
must_haves:
  truths:
    - "useSessionFinance exposes enough inventory-style batch metadata for a manual picker and manual allocation editor"
    - "Available manual picker batches stay ordered cheapest-first by default and carry stable tube identity, brand, remaining stock, cost per tube, and notes"
    - "Saved manual allocations can be rehydrated into an editable row shape without inventing a second persistence contract"
    - "Manual allocation saves continue to use the existing full-replacement save path through saveUsageAllocation"
    - "Helper-focused unit coverage protects manual row shaping and reload behavior"
  artifacts:
    - path: "badminton-v2/src/hooks/useSessionFinance.ts"
      provides: "manual allocation batch metadata and save/hydration helpers for finance UI"
      contains: "manual"
    - path: "badminton-v2/src/__tests__/useSessionFinance.test.ts"
      provides: "manual allocation helper coverage"
      contains: "manual"
---

<objective>
Enrich the finance hook so Phase 17 UI can render and save manual batch allocations using inventory-style details and editable reload data.

Purpose: The picker dialog and selected-row editor both depend on richer batch/allocation view models than Phase 16 exposes today. Landing that contract first keeps the finance page thin and prevents UI duplication of inventory math.
Output: `useSessionFinance` provides manual-picker batch metadata, editable manual allocation row shaping, and tests that protect the manual reload/save contract.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-manual-batch-allocation-ui/17-CONTEXT.md
@.planning/phases/17-manual-batch-allocation-ui/17-RESEARCH.md
@.planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useSessionFinance manual batch and allocation view models</name>
  <files>
    badminton-v2/src/hooks/useSessionFinance.ts,
    badminton-v2/src/__tests__/useSessionFinance.test.ts
  </files>
  <read_first>
    - badminton-v2/src/hooks/useSessionFinance.ts
    - badminton-v2/src/__tests__/useSessionFinance.test.ts
    - badminton-v2/src/hooks/useShuttleBatches.ts
    - badminton-v2/src/views/InventoryView.tsx
  </read_first>
  <action>
Refactor the finance hook's manual-side data contracts so the UI can render inventory-style details without re-deriving them in the view.

Recommended direction:
- extend the available finance batch model to include `notes`
- expose a picker-friendly batch list already ordered cheapest-first
- add a helper or derived row shape that can merge saved manual allocations with batch metadata for editable reload
- keep stable tube identity aligned with the existing inventory numbering rules

Add helper-focused unit coverage for:
- cheapest-first batch ordering remains stable
- saved manual allocation rows can be shaped back into editable rows with inventory-style details
- manual save input still compiles down to the existing `shuttle_usage` row contract
  </action>
  <done>
    - Hook data is rich enough for the picker and the manual editor
    - Tests protect manual row shaping and default ordering behavior
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve the manual save and reload seam through the existing replacement contract</name>
  <files>badminton-v2/src/hooks/useSessionFinance.ts</files>
  <read_first>
    - badminton-v2/src/hooks/useSessionFinance.ts
    - .planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
  </read_first>
  <action>
Keep the Phase 16 save boundary intact while making it easier for Phase 17 UI to reopen and edit manual allocations:

- preserve `saveUsageAllocation({ allocationMode: 'manual', rows })` as the canonical manual save path
- ensure saved manual allocations remain readable after refetch and can seed editable draft rows
- avoid adding duplicate persistence logic or a parallel draft table

Do not introduce Phase 18's blocking validation rules here. This plan is about data shape and reliable reload, not final enforcement.
  </action>
  <done>
    - Manual mode keeps one canonical save path
    - Saved rows can be read back into the Phase 17 editor cleanly
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run test:unit` passes
- `npm.cmd run build` exits 0
- `grep -n "notes" badminton-v2/src/hooks/useSessionFinance.ts` returns manual batch or allocation metadata lines
- `grep -n "manual" badminton-v2/src/__tests__/useSessionFinance.test.ts` returns manual helper coverage lines
</verification>

<success_criteria>
- Finance hook exposes inventory-style metadata needed by manual mode
- Manual reload/edit behavior has a stable data contract
- Existing manual save contract remains the single source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/17-manual-batch-allocation-ui/17-01-SUMMARY.md`
</output>
