---
phase: 17-manual-batch-allocation-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - badminton-v2/src/views/FinanceDetailView.tsx
  - badminton-v2/src/components/ManualAllocationEditor.tsx
autonomous: true
requirements:
  - MAN-02
  - MAN-03
  - MAN-04
  - MAN-05
  - MAN-06
must_haves:
  truths:
    - "Manual mode on the finance page shows an editable selected-batch table instead of the Phase 16 placeholder shell"
    - "Selected rows keep the order they were added"
    - "Per-row shuttle counts update the total immediately as the QM edits"
    - "Removing a row requires confirmation rather than deleting immediately"
    - "Reopening a session with saved manual allocations restores those rows directly into the editable table"
    - "Saved rows and newly added rows are rendered as one current editable state without visual distinction"
  artifacts:
    - path: "badminton-v2/src/views/FinanceDetailView.tsx"
      provides: "manual finance-page integration for picker, editor, and save flow"
      contains: "Manual batch allocation"
    - path: "badminton-v2/src/components/ManualAllocationEditor.tsx"
      provides: "editable manual allocation table and live total behavior"
      contains: "shuttles"
---

<objective>
Integrate the picker and editable manual allocation table into the finance page so QMs can build, save, and reopen manual allocations directly in manual mode.

Purpose: Phase 17 is complete only when the manual flow works end-to-end on the finance page: add batches, edit counts, remove rows with confirmation, save manual allocations, and reopen them for editing later.
Output: `FinanceDetailView` replaces the placeholder shell with a working manual allocation editor backed by the picker dialog and the existing finance save contract.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-manual-batch-allocation-ui/17-CONTEXT.md
@.planning/phases/17-manual-batch-allocation-ui/17-RESEARCH.md
@.planning/phases/17-manual-batch-allocation-ui/17-01-PLAN.md
@.planning/phases/17-manual-batch-allocation-ui/17-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the manual placeholder with an editable allocation table</name>
  <files>
    badminton-v2/src/views/FinanceDetailView.tsx,
    badminton-v2/src/components/ManualAllocationEditor.tsx
  </files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
    - badminton-v2/src/components/RosterPanel.tsx
  </read_first>
  <action>
Create the manual allocation editor UI and integrate it into manual mode.

Recommended behavior:
- selected rows render as a table with inventory-style details and per-row shuttle count inputs
- row order stays in add order
- total logged shuttles updates immediately from current draft counts
- each row includes a remove action with lightweight confirmation before deletion

Reuse the repo's existing lightweight confirmation style where appropriate instead of adding new modal confirmation infrastructure.
  </action>
  <done>
    - Manual mode no longer shows the Phase 16 placeholder
    - Selected rows are editable and totals update live
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire picker, save, and reload behavior into FinanceDetailView</name>
  <files>badminton-v2/src/views/FinanceDetailView.tsx</files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/components/ManualBatchPickerDialog.tsx
    - badminton-v2/src/components/ManualAllocationEditor.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Integrate the manual editor flow end-to-end:

- add an `Add batch` trigger that opens the picker dialog
- insert selected batches into the editable table immediately
- seed the editable table from saved manual allocations when the page loads or when manual mode is reopened
- save current draft rows through `saveUsageAllocation({ allocationMode: 'manual', rows })`
- keep saved and newly added rows visually unified

Do not overreach into Phase 18's blocking validation rules. The Phase 17 goal is construction, editing, save, and reload.
  </action>
  <done>
    - QM can add, edit, save, and reopen manual allocations from the finance page
    - Reloaded manual rows behave like the live editable state
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run build` exits 0
- `npm.cmd run lint` exits 0
- `grep -n "saveUsageAllocation" badminton-v2/src/views/FinanceDetailView.tsx` returns manual save integration
- `grep -n "Add batch" badminton-v2/src/views/FinanceDetailView.tsx` returns the picker trigger
- `grep -n "Sure?" badminton-v2/src/components/ManualAllocationEditor.tsx` or equivalent confirmation UI returns row-removal confirmation behavior
</verification>

<success_criteria>
- Finance page supports manual batch selection, editing, save, and reload
- Totals update immediately from per-row counts
- Removal requires confirmation
- Reopened manual allocations restore directly into the editable table
</success_criteria>

<output>
After completion, create `.planning/phases/17-manual-batch-allocation-ui/17-03-SUMMARY.md`
</output>
