---
phase: 15-split-stats-aggregation
plan: 02
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - badminton-v2/src/views/TodayView.tsx
  - badminton-v2/src/views/SessionPlayerDetailView.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - COMP-01
must_haves:
  truths:
    - "TodayView leaderboard counts every result row for wins and games"
    - "SessionPlayerDetailView leaderboard counts every result row for wins and games"
    - "Leaderboard display text remains unchanged (`{wins}W {losses}L`)"
    - "Legacy one-row matches still produce the same leaderboard totals as before"
  artifacts:
    - path: "badminton-v2/src/views/TodayView.tsx"
      provides: "split-aware today leaderboard aggregation"
      contains: "computeStatsFromResults"
    - path: "badminton-v2/src/views/SessionPlayerDetailView.tsx"
      provides: "split-aware session leaderboard aggregation"
      contains: "computeStatsFromResults"
---

<objective>
Update the session and today leaderboard surfaces to aggregate all game-level result rows via the shared helper.

Purpose: These are the visible v1.3 stats regressions users will notice first. They should be aligned with the same math used by all-time profile stats.
Output: Both leaderboard fetch functions use the shared helper and preserve the existing UI shape.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-split-stats-aggregation/15-CONTEXT.md
@.planning/phases/15-split-stats-aggregation/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TodayView session leaderboard math</name>
  <files>badminton-v2/src/views/TodayView.tsx</files>
  <read_first>
    - badminton-v2/src/views/TodayView.tsx
    - badminton-v2/src/lib/matchResults.ts
  </read_first>
  <action>
Replace the in-function `getLegacyWinningPairIndex` loop with `computeStatsFromResults`.

Recommended approach:
- keep `statsMap` seeded for all registered players
- for each completed match, call `computeStatsFromResults(match)`
- merge each returned player stat into `statsMap`

Do not change sort order, `mostImproved` logic, loading states, or the displayed `W/L` label text.
  </action>
  <done>
    - Today leaderboard reflects split rows correctly
    - UI text and ranking behavior remain unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate SessionPlayerDetailView leaderboard math</name>
  <files>badminton-v2/src/views/SessionPlayerDetailView.tsx</files>
  <read_first>
    - badminton-v2/src/views/SessionPlayerDetailView.tsx
    - badminton-v2/src/lib/matchResults.ts
  </read_first>
  <action>
Apply the same helper-based merge pattern to `fetchLeaderboard(sessionId)`.

Keep the session leaderboard UI unchanged. Only the numbers should change for split sessions.
  </action>
  <done>
    - Session leaderboard matches TodayView math
    - No UI or realtime regression introduced
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run test:unit` passes
- `npm.cmd run build` exits 0
- `grep -n "computeStatsFromResults" badminton-v2/src/views/TodayView.tsx` returns the import and usage
- `grep -n "computeStatsFromResults" badminton-v2/src/views/SessionPlayerDetailView.tsx` returns the import and usage
</verification>

<success_criteria>
- Today and session leaderboards aggregate all result rows
- Display remains `{wins}W {losses}L`
- Legacy matches remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-split-stats-aggregation/15-02-SUMMARY.md`
</output>
