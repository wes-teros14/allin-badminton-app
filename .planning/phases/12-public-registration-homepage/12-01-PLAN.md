---
phase: 12-public-registration-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - badminton-v2/src/views/HomeView.tsx
  - badminton-v2/tests/public-homepage.spec.ts
  - .planning/phases/12-public-registration-homepage/12-01-SUMMARY.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - AUTH-01
  - AUTH-02
  - INVITE-01
  - INVITE-02
must_haves:
  truths:
    - Signed-out visitors opening / see a simple public homepage for the badminton group app.
    - The public homepage has a clear Register action.
    - Clicking Register starts existing Google OAuth and redirects back to /.
    - Signed-in users still see the existing authenticated homepage experience.
    - The /register invite-token path remains reachable and compatible.
  artifacts:
    - path: badminton-v2/src/views/HomeView.tsx
      provides: Public signed-out homepage branch and preserved signed-in branch.
      min_lines: 110
    - path: badminton-v2/tests/public-homepage.spec.ts
      provides: E2E smoke coverage for public homepage, OAuth CTA, signed-in branch, and invite compatibility.
      min_lines: 60
  key_links:
    - from: badminton-v2/src/views/HomeView.tsx
      to: Supabase Google OAuth
      via: Register button onClick calls supabase.auth.signInWithOAuth with provider google and redirectTo /
    - from: badminton-v2/src/App.tsx
      to: badminton-v2/src/views/HomeView.tsx
      via: Existing root route remains unchanged.
    - from: badminton-v2/src/App.tsx
      to: badminton-v2/src/views/RegisterView.tsx
      via: Existing /register route remains unchanged.
user_setup: []
---

<objective>
Signed-out visitors can start normal registration from the root homepage without needing an invite link, while signed-in users and existing invite-token registration keep their current behavior.
</objective>

<context>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/12-public-registration-homepage/12-CONTEXT.md
    - .planning/phases/12-public-registration-homepage/12-RESEARCH.md
    - badminton-v2/src/views/HomeView.tsx
    - badminton-v2/src/App.tsx
    - badminton-v2/src/views/RegisterView.tsx
    - badminton-v2/src/contexts/AuthContext.tsx
    - badminton-v2/tests/registration-limit.spec.ts
  </read_first>
  <constraints>
    - Work from badminton-v2/ for npm commands.
    - Do not add npm packages.
    - Do not alter generated output in badminton-v2/dist/.
    - Do not remove or rewrite invite-token registration code.
    - Keep Google OAuth as the only auth method.
  </constraints>
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Add public signed-out homepage branch</name>
    <files>badminton-v2/src/views/HomeView.tsx</files>
    <action>
      Replace only the `if (!user)` return branch in `HomeView` with a simple public homepage that gives basic app information and presents a registration-oriented primary action per D-01 and D-02. Rename the local click handler from `signIn` to a registration-oriented name such as `registerWithGoogle`, but keep the implementation on existing Supabase Google OAuth per D-03 and AUTH-02. Preserve the existing `redirectTo` behavior that uses `import.meta.env.VITE_APP_URL ?? window.location.origin` and returns to `/` per D-05. The primary button text must be `Register with Google` or an equally clear registration-oriented label, and the page must not include any public form fields, intermediate confirmation step, or alternate auth provider per D-04. Do not change the loading branch or the signed-in branch that renders `Welcome back!` and `BulletinBoard`, preserving AUTH-01 and D-06. Do not modify `/register`, invite tokens, or `RegisterView`, preserving D-07 and D-08.
    </action>
    <verify>
      <automated>npm run lint</automated>
      <manual>Open / signed out and confirm the first screen reads as a simple badminton app homepage with a Register action, not a generic sign-in wall.</manual>
    </verify>
    <done>
      REG-01, REG-02, REG-03, AUTH-01, AUTH-02, D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 are satisfied in `HomeView` without changing authenticated homepage or invite-token behavior.
    </done>
  </task>

  <task type="auto">
    <name>Task 2: Add public homepage E2E coverage</name>
    <files>badminton-v2/tests/public-homepage.spec.ts</files>
    <action>
      Create a Playwright spec following the helper style in `badminton-v2/tests/registration-limit.spec.ts`. Add a local `ensureSignedOut(page)` helper using the existing DEV panel pattern. Test that a signed-out visitor at `/` sees the public homepage copy and a visible Register action, and does not see a public registration form. Test that clicking Register attempts the existing Google OAuth path without relying on real Google by waiting for either a navigation away from the app or stubbing the Supabase auth request if the current app behavior allows a stable browser-side interception. Add a signed-in smoke test using the existing DEV panel helper pattern to sign in as a seeded player and assert `Welcome back!` remains visible, covering AUTH-01 and D-06. Add an invite compatibility smoke test that visits `/register` with no token and asserts the existing `Registration Closed` state remains visible, covering INVITE-01 and D-07 without modifying `RegisterView`.
    </action>
    <verify>
      <automated>npm run test:e2e -- public-homepage.spec.ts</automated>
      <automated>npm run lint</automated>
    </verify>
    <done>
      The new Playwright spec covers REG-01, REG-02, REG-03, AUTH-01, AUTH-02, INVITE-01, and INVITE-02 with deterministic checks that do not require a real Google login.
    </done>
  </task>

  <task type="auto">
    <name>Task 3: Run phase verification commands and record execution summary</name>
    <files>.planning/phases/12-public-registration-homepage/12-01-SUMMARY.md</files>
    <action>
      Run the focused phase checks from `badminton-v2/`: `npm run lint` and `npm run test:e2e -- public-homepage.spec.ts`. If the Playwright spec requires seeded users for the signed-in branch and the test environment reports missing seed data, run `npm run seed -- --sessions 2` and retry once. Create `12-01-SUMMARY.md` with the commands run, pass/fail result, files changed, deviations, and any environment limitations. Do not mark success if the signed-out homepage, Register CTA, signed-in branch, or `/register` compatibility smoke test is unverified.
    </action>
    <verify>
      <automated>npm run lint</automated>
      <automated>npm run test:e2e -- public-homepage.spec.ts</automated>
    </verify>
    <done>
      Lint and focused E2E checks are passing, or any external environment blocker is recorded clearly in `12-01-SUMMARY.md` with no unverified behavior hidden.
    </done>
  </task>
</tasks>

<verification>
  <automated>
    cd badminton-v2
    npm run lint
    npm run test:e2e -- public-homepage.spec.ts
  </automated>
  <manual>
    Review the signed-out `/` page visually after execution to confirm it is simple and app-focused, with a clear Register action and no public form.
  </manual>
</verification>

<success_criteria>
- A signed-out visitor opening `/` sees a public homepage instead of a generic immediate Google sign-in prompt.
- The public homepage presents a clear Register action that starts existing Google OAuth directly.
- OAuth redirect target for the Register action remains `/`.
- Signed-in users opening `/` continue to see the existing authenticated homepage experience.
- `/register` remains routed to the existing invite-token registration view and tokenless compatibility state still appears.
- `npm run lint` and `npm run test:e2e -- public-homepage.spec.ts` pass, or any environment-only blocker is documented in the execution summary.
</success_criteria>

<source_audit>
  <goal status="covered">Signed-out visitors can start registration from the root homepage without invite-link dependence, while signed-in users and Google OAuth behavior stay intact.</goal>
  <requirements>
    <requirement id="REG-01" status="covered">Task 1 changes signed-out `/`; Task 2 tests signed-out root homepage.</requirement>
    <requirement id="REG-02" status="covered">Task 1 adds clear Register CTA; Task 2 tests CTA visibility.</requirement>
    <requirement id="REG-03" status="covered">Task 1 uses existing Supabase Google OAuth directly; Task 2 tests OAuth initiation path.</requirement>
    <requirement id="AUTH-01" status="covered">Task 1 preserves signed-in branch; Task 2 tests signed-in `Welcome back!` smoke path.</requirement>
    <requirement id="AUTH-02" status="covered">Task 1 prohibits new auth methods/forms; Task 2 checks no form and Google-only CTA.</requirement>
    <requirement id="INVITE-01" status="covered">Task 1 avoids `/register`; Task 2 tests existing tokenless invite state remains reachable.</requirement>
    <requirement id="INVITE-02" status="covered">Task 1 starts normal onboarding from `/` without token dependency.</requirement>
  </requirements>
  <context_decisions>
    <decision id="D-01" status="covered">Task 1 simple informational homepage.</decision>
    <decision id="D-02" status="covered">Task 1 copy identifies badminton group app and registration entry.</decision>
    <decision id="D-03" status="covered">Task 1 Register button starts Google OAuth.</decision>
    <decision id="D-04" status="covered">Task 1 forbids public form or intermediate confirmation.</decision>
    <decision id="D-05" status="covered">Task 1 preserves redirect to `/`.</decision>
    <decision id="D-06" status="covered">Task 1 preserves signed-in branch; Task 2 tests it.</decision>
    <decision id="D-07" status="covered">Task 1 avoids invite code; Task 2 checks `/register` compatibility.</decision>
    <decision id="D-08" status="covered">Task 1 does not modify invite behavior except avoiding dependence for homepage onboarding.</decision>
  </context_decisions>
  <research status="covered">Uses existing route/auth stack and validation architecture from 12-RESEARCH.md.</research>
</source_audit>

<threat_model>
  <threat stride="spoofing" disposition="mitigate">
    Register CTA must call existing Supabase Google OAuth, not a custom redirect or form.
  </threat>
  <threat stride="information_disclosure" disposition="mitigate">
    No public form fields are introduced, so the homepage collects no credentials or registration data.
  </threat>
  <threat stride="elevation_of_privilege" disposition="mitigate">
    AdminRoute, AuthContext, and invite-token validation are left unchanged; tests smoke the existing signed-in and `/register` paths.
  </threat>
</threat_model>

<output>
  <files_created>
    - badminton-v2/tests/public-homepage.spec.ts
    - .planning/phases/12-public-registration-homepage/12-01-SUMMARY.md
  </files_created>
  <files_modified>
    - badminton-v2/src/views/HomeView.tsx
  </files_modified>
</output>
