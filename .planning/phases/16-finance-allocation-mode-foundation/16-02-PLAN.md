---
phase: 16-finance-allocation-mode-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 16-01
files_modified:
  - badminton-v2/src/hooks/useSessionFinance.ts
  - badminton-v2/src/__tests__/useSessionFinance.test.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-03
  - COMP-02
must_haves:
  truths:
    - "useSessionFinance loads explicit allocation mode from the finance contract and falls back to 'auto' defensively"
    - "The current auto save path remains available and continues to allocate cheapest-first without behavioral change"
    - "The shared save path treats every usage save as full replacement of the session's shuttle_usage rows"
    - "The hook can represent both auto total input and manual per-batch allocation input even before the manual picker UI exists"
    - "Unit coverage protects mode normalization and replacement-oriented helper behavior"
  artifacts:
    - path: "badminton-v2/src/hooks/useSessionFinance.ts"
      provides: "mode-aware finance hook and shared save contract"
      contains: "allocationMode"
    - path: "badminton-v2/src/__tests__/useSessionFinance.test.ts"
      provides: "helper coverage for mode normalization and replacement-safe finance helpers"
      contains: "allocationMode"
---

<objective>
Refactor `useSessionFinance` into a mode-aware finance contract while preserving today's auto save behavior as a compatibility wrapper.

Purpose: Phase 17 needs a stable hook contract for manual per-batch allocation, but Phase 16 must not regress the current total-shuttles auto flow. This plan centralizes the shared save semantics and keeps the old auto path intact on top of the new foundation.
Output: `useSessionFinance` exposes explicit mode and a shared save contract, while `logUsage(totalShuttles)` still works as before.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
@.planning/phases/16-finance-allocation-mode-foundation/16-RESEARCH.md
@.planning/phases/16-finance-allocation-mode-foundation/16-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mode-aware types and replacement helpers to useSessionFinance</name>
  <files>
    badminton-v2/src/hooks/useSessionFinance.ts,
    badminton-v2/src/__tests__/useSessionFinance.test.ts
  </files>
  <read_first>
    - badminton-v2/src/hooks/useSessionFinance.ts
    - badminton-v2/src/__tests__/useSessionFinance.test.ts
    - badminton-v2/src/types/database.ts
  </read_first>
  <action>
Refactor the hook around explicit mode-aware contracts. Add typed models such as:
- `AllocationMode = 'auto' | 'manual'`
- `ManualUsageInput` / `ManualUsageRowInput`
- a normalized helper for resolving mode from DB rows
- a helper that turns mode-aware save input into `shuttle_usage` insert rows

Preserve `allocateCheapestFirst` unchanged. Keep the current `logUsage(totalShuttles)` API available by routing it through the new shared save path.

Add helper-focused unit tests for:
- missing/null mode normalizes to `auto`
- auto saves still allocate cheapest-first
- manual row input can be represented as per-batch `shuttle_usage` rows
  </action>
  <done>
    - Hook types can represent both auto and manual allocation input
    - Tests cover normalization and row-building behavior
  </done>
</task>

<task type="auto">
  <name>Task 2: Centralize mode persistence and full-replacement save behavior</name>
  <files>badminton-v2/src/hooks/useSessionFinance.ts</files>
  <read_first>
    - badminton-v2/src/hooks/useSessionFinance.ts
    - badminton-v2/src/types/database.ts
  </read_first>
  <action>
Update the hook so it:
- reads `shuttle_allocation_mode` from the finance contract
- exposes the active `allocationMode`
- persists mode changes on the session row
- uses one shared delete-then-insert path for both auto and manual saves
- refreshes from the backend contract after writes instead of maintaining divergent local truth

Keep current return data such as `usageAllocations`, `shuttleCost`, and `totalShuttlesLogged` intact for existing readers.
  </action>
  <done>
    - Full replacement save semantics are centralized in one path
    - Existing auto callers keep working
    - Manual mode can be loaded and saved by contract, even before the Phase 17 picker lands
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run test:unit` passes
- `npm.cmd run build` exits 0
- `grep -n "allocationMode" badminton-v2/src/hooks/useSessionFinance.ts` returns state/type/return lines
- `grep -n "logUsage" badminton-v2/src/hooks/useSessionFinance.ts` still returns the compatibility wrapper
</verification>

<success_criteria>
- `useSessionFinance` becomes mode-aware without breaking current auto callers
- Full replacement save semantics are preserved
- Helper coverage protects normalization and auto compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/16-finance-allocation-mode-foundation/16-02-SUMMARY.md`
</output>
