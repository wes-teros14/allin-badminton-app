---
phase: 16-finance-allocation-mode-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - badminton-v2/supabase/migrations/065_add_shuttle_allocation_mode.sql
  - badminton-v2/src/types/database.ts
autonomous: true
requirements:
  - FIN-01
  - FIN-03
  - COMP-02
must_haves:
  truths:
    - "Finance allocation mode is stored explicitly on the session finance record instead of being inferred from shuttle_usage rows"
    - "The new mode contract defaults existing and newly read records to 'auto'"
    - "Current automatic finance records remain readable and editable without a separate data migration"
    - "The database type contract exposes the new allocation mode field on sessions and on get_session_finance results"
  artifacts:
    - path: "badminton-v2/supabase/migrations/065_add_shuttle_allocation_mode.sql"
      provides: "explicit session-level shuttle allocation mode schema"
      contains: "shuttle_allocation_mode"
    - path: "badminton-v2/src/types/database.ts"
      provides: "typed access to shuttle allocation mode in sessions and finance RPC results"
      contains: "shuttle_allocation_mode"
---

<objective>
Add the schema and typed backend contract for explicit finance allocation modes while preserving auto mode as the default.

Purpose: The rest of Phase 16 depends on a durable source of truth for `auto` versus `manual`. Landing the DB contract first avoids hook- and UI-level guesswork and locks compatibility behavior before the save path is refactored.
Output: a migration adds the mode field, and `database.ts` reflects it for both `sessions` and `get_session_finance`.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
@.planning/phases/16-finance-allocation-mode-foundation/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit shuttle allocation mode to the sessions contract</name>
  <files>badminton-v2/supabase/migrations/065_add_shuttle_allocation_mode.sql</files>
  <read_first>
    - badminton-v2/supabase/migrations/002_create_sessions.sql
    - badminton-v2/supabase/migrations/059_add_personal_share_override_to_sessions.sql
    - badminton-v2/supabase/migrations/060_update_get_session_finance_for_personal_share.sql
  </read_first>
  <action>
Create a new migration that:
- creates a Postgres enum `shuttle_allocation_mode` with values `auto` and `manual`
- adds `sessions.shuttle_allocation_mode shuttle_allocation_mode not null default 'auto'`
- updates `get_session_finance` so its return table includes `shuttle_allocation_mode`
- returns the mode from the finance base query without changing the current cost, revenue, or profit formulas

Keep the current auto behavior intact. This migration is adding explicit mode metadata, not changing the meaning of existing `shuttle_usage` rows.
  </action>
  <done>
    - Migration adds the new enum and column
    - `get_session_finance` returns the new mode field
    - Existing rows normalize to `auto` through the DB contract
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync generated database types to the new finance mode contract</name>
  <files>badminton-v2/src/types/database.ts</files>
  <read_first>
    - badminton-v2/src/types/database.ts
    - badminton-v2/supabase/migrations/065_add_shuttle_allocation_mode.sql
  </read_first>
  <action>
Update `database.ts` so:
- `sessions.Row`, `Insert`, and `Update` include `shuttle_allocation_mode`
- the `get_session_finance` function return type includes `shuttle_allocation_mode`
- the new enum is represented in `Database["public"]["Enums"]`

Do not make unrelated formatting or contract changes in the generated file.
  </action>
  <done>
    - TypeScript knows about the new mode field everywhere it is read or written
    - Finance hooks can consume the new mode without `any` casts
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run build` exits 0
- `grep -n "shuttle_allocation_mode" badminton-v2/src/types/database.ts` returns session fields plus RPC return entries
- `grep -n "shuttle_allocation_mode" badminton-v2/supabase/migrations/065_add_shuttle_allocation_mode.sql` returns enum, column, and RPC lines
</verification>

<success_criteria>
- Explicit finance allocation mode exists at the session-contract level
- Old finance records still resolve to `auto`
- The typed backend contract exposes mode without changing current finance totals
</success_criteria>

<output>
After completion, create `.planning/phases/16-finance-allocation-mode-foundation/16-01-SUMMARY.md`
</output>
