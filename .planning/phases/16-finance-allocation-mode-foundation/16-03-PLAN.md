---
phase: 16-finance-allocation-mode-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - badminton-v2/src/views/FinanceDetailView.tsx
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - COMP-02
must_haves:
  truths:
    - "FinanceDetailView shows an explicit auto/manual mode switch on the session finance page"
    - "Auto mode continues to show the current total-shuttles entry flow and save path unchanged"
    - "Manual mode branches into a distinct finance section shell instead of reusing the auto total input UI"
    - "Switching modes persists the active allocation mode but does not preserve a hidden draft from the other mode"
    - "Existing saved allocation rows remain readable in either mode without migration errors"
  artifacts:
    - path: "badminton-v2/src/views/FinanceDetailView.tsx"
      provides: "user-visible finance mode switch and mode-aware section rendering"
      contains: "allocation mode"
---

<objective>
Integrate explicit finance allocation modes into `FinanceDetailView` while keeping the current automatic finance workflow unchanged and compatible.

Purpose: Phase 16 is not finished until the session finance page can express the new mode boundary. This plan adds the visible mode switch and mode-aware rendering shell, while deliberately leaving the batch-picker and manual row editor for Phase 17.
Output: `FinanceDetailView` persists and displays `auto` / `manual` mode, keeps the existing auto entry flow intact, and branches manual mode into a dedicated shell ready for the next phase.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
@.planning/phases/16-finance-allocation-mode-foundation/16-RESEARCH.md
@.planning/phases/16-finance-allocation-mode-foundation/16-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit mode switch UI to FinanceDetailView</name>
  <files>badminton-v2/src/views/FinanceDetailView.tsx</files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Add an explicit `auto` / `manual` mode switch near the Shuttle Usage section header. Use the new hook contract from Plan 02 to:
- read the active mode
- persist mode changes
- disable the switch while mode writes are in flight

Switching mode must not alter allocation rows until the user performs the next save in that mode. The mode control should therefore persist the active mode but not attempt any hidden auto-to-manual conversion.
  </action>
  <done>
    - Session finance page shows the explicit mode switch
    - Mode changes persist through the hook contract
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve auto UI and branch manual mode into a dedicated shell</name>
  <files>badminton-v2/src/views/FinanceDetailView.tsx</files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
    - .planning/phases/16-finance-allocation-mode-foundation/16-CONTEXT.md
  </read_first>
  <action>
Render the Shuttle Usage card by mode:

- In `auto` mode:
  - keep the current "Total Shuttles Used" input and save/update button behavior
  - continue to call the preserved auto save wrapper from the hook
  - keep current allocation-table rendering for saved rows

- In `manual` mode:
  - render a dedicated shell titled for manual allocation
  - show any existing saved batch allocation rows using the same allocation display table
  - render a clear "manual batch selection arrives in Phase 17" empty-state or helper note when no manual rows exist yet
  - do not show the auto total-shuttles input in this branch

Do not add brand search, batch picker, or per-row editing in this plan.
  </action>
  <done>
    - Auto mode remains unchanged for current users
    - Manual mode is visible and mode-specific without overreaching into Phase 17
    - Existing finance records stay readable regardless of mode
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run build` exits 0
- `npm.cmd run lint` exits 0
- `grep -n "manual" badminton-v2/src/views/FinanceDetailView.tsx` returns mode-branch rendering lines
- `grep -n "totalShuttles" badminton-v2/src/views/FinanceDetailView.tsx` returns only the auto-mode input path
</verification>

<success_criteria>
- Finance page exposes explicit auto/manual mode
- Auto mode remains behaviorally unchanged
- Manual mode has a dedicated shell ready for Phase 17 without crashing or reusing the auto input path
</success_criteria>

<output>
After completion, create `.planning/phases/16-finance-allocation-mode-foundation/16-03-SUMMARY.md`
</output>
