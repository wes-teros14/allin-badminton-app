---
phase: 11-payment-migration
plan: "01"
wave: 1
type: execute
autonomous: true
subsystem: ui
tags: [react, typescript]
depends_on: []
requires: []
provides: [payment-panel-in-finance-detail]
affects:
  - badminton-v2/src/views/FinanceDetailView.tsx
files_modified:
  - badminton-v2/src/views/FinanceDetailView.tsx
requirements_completed: [PAY-01]

must_haves:
  truths:
    - "Admin can expand a Payment Status card on the FinanceDetailView and toggle each player Paid or Unpaid"
    - "The payment card shows 'X paid · Y unpaid' in the collapsed header"
    - "Toggling paid status persists (uses existing updatePaid from useRoster)"
  artifacts:
    - path: "badminton-v2/src/views/FinanceDetailView.tsx"
      provides: "RosterPanel paymentOnly block rendered below P&L Summary card"
      contains: "<RosterPanel sessionId={sessionId"
  key_links:
    - from: "FinanceDetailView.tsx"
      to: "RosterPanel paymentOnly mode"
      via: "import RosterPanel; render <RosterPanel sessionId={sessionId} paymentOnly />"
      pattern: "paymentOnly"
---

<objective>
Add the payment management panel to FinanceDetailView so Admin can mark players Paid/Unpaid from the Finance page.

Purpose: PAY-01 — payment controls belong exclusively on the Finance page. RosterPanel already has a complete `paymentOnly` mode (lines 49–92 of RosterPanel.tsx); this plan wires it into FinanceDetailView as a new fourth card below the P&L Summary.

Output: FinanceDetailView renders a collapsible "Payment Status" card with per-player Paid/Unpaid toggles, sourced from useRoster via RosterPanel.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/1Wes/all-in-badminton-app/.planning/ROADMAP.md

<interfaces>
<!-- RosterPanel.tsx — Props interface (line 18–22) -->
```typescript
interface Props {
  sessionId: string
  editable?: boolean
  paymentOnly?: boolean   // <-- this mode is the one we need
}
```

<!-- RosterPanel paymentOnly render path (lines 49–92):
  - Renders a <Card> with collapsible header showing "Payment Status — X paid · Y unpaid"
  - Expands to list all players with Paid/Unpaid toggle buttons
  - Calls updatePaid(player.registrationId, boolean) from useRoster
  - No other props needed — sessionId is sufficient
-->

<!-- FinanceDetailView.tsx current structure (lines 75–246):
  - Line 7:  import { useSessionFinance } from '@/hooks/useSessionFinance'
  - Line 33: const { sessionId } = useParams<{ sessionId: string }>()
  - Line 76: <div className="p-6 max-w-lg mx-auto space-y-6">
  - Card 1 (line 96):  Shuttle Usage
  - Card 2 (line 163): Court Cost
  - Card 3 (line 197): P&L Summary   <-- insert new card AFTER this, before closing </div>
  - Line 245: closing </div> of the page wrapper
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import RosterPanel and render payment card in FinanceDetailView</name>
  <files>badminton-v2/src/views/FinanceDetailView.tsx</files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx — understand current import list and JSX structure (lines 1–16 for imports, lines 75–246 for JSX)
    - badminton-v2/src/components/RosterPanel.tsx — confirm the named export is `RosterPanel` and paymentOnly prop behavior (lines 1–5, 18–22, 49–92)
  </read_first>
  <action>
1. Add the RosterPanel import on line 16 (after the Table imports block):
   ```typescript
   import { RosterPanel } from '@/components/RosterPanel'
   ```

2. Inside the return JSX, after the closing `</Card>` of the P&L Summary section (line 243) and before the outer closing `</div>` (line 245), insert a new Section 4 comment and the RosterPanel:
   ```tsx
   {/* Section 4: Payment Status */}
   <RosterPanel sessionId={sessionId ?? ''} paymentOnly />
   ```

   The `sessionId` variable is already destructured from useParams on line 33 — use it directly. The `??  ''` guard is already the pattern used on line 34 with `useSessionFinance`.

3. No other changes. Do not modify any existing cards, hooks, or state.
  </action>
  <verify>
    <automated>cd C:/1Wes/all-in-badminton-app/badminton-v2 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `FinanceDetailView.tsx` imports RosterPanel from '@/components/RosterPanel'
    - A `<RosterPanel sessionId={sessionId ?? ''} paymentOnly />` element exists after the P&L Summary card
    - `npx tsc --noEmit` reports zero errors for this file
    - grep confirms: `grep -n "paymentOnly" badminton-v2/src/views/FinanceDetailView.tsx` returns a result
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Supabase RLS | updatePaid writes go through existing useRoster hook; RLS must restrict paid updates to admin role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | updatePaid (useRoster) | accept | Existing RLS on session_registrations table already enforced in phase 08; no new surface added here |
| T-11-02 | Elevation of Privilege | FinanceDetailView route | accept | Finance routes are already admin-gated by existing route guards; no new route added |
</threat_model>

<verification>
After task completes:

```bash
grep -n "RosterPanel" badminton-v2/src/views/FinanceDetailView.tsx
# Expected: import line + JSX usage line

grep -n "paymentOnly" badminton-v2/src/views/FinanceDetailView.tsx
# Expected: one result on the <RosterPanel ... paymentOnly /> line

cd badminton-v2 && npx tsc --noEmit
# Expected: 0 errors
```
</verification>

<success_criteria>
- FinanceDetailView renders a fourth card: "Payment Status — X paid · Y unpaid"
- Clicking the header expands the card showing all registered players
- Each player row has Paid (green) / Unpaid (red) toggle buttons
- Toggling a player updates the database (via existing updatePaid in useRoster)
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-payment-migration/11-01-SUMMARY.md`
</output>
