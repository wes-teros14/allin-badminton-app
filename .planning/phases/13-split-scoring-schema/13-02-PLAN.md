---
phase: 13-split-scoring-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01-PLAN.md
files_modified:
  - badminton-v2/src/lib/matchResults.ts
  - badminton-v2/src/hooks/useAdminActions.ts
  - badminton-v2/src/components/CourtCard.tsx
  - badminton-v2/src/hooks/usePlayerStats.ts
  - badminton-v2/src/hooks/usePlayerSchedule.ts
  - badminton-v2/src/views/TodayView.tsx
  - badminton-v2/src/views/SessionPlayerDetailView.tsx
  - .planning/phases/13-split-scoring-schema/13-02-SUMMARY.md
autonomous: true
requirements:
  - FMT-01
  - RES-03
  - COMP-01
must_haves:
  truths:
    - Current one-game writers explicitly align to the new game-level result contract without changing UX.
    - Current readers stop relying on an unordered `match_results[0]` assumption.
    - One centralized helper defines how legacy one-game rows map to game 1 and effective winner semantics.
    - Phase 13 does not implement split-result choices or multi-row leaderboard formulas beyond compatibility normalization.
  artifacts:
    - path: badminton-v2/src/lib/matchResults.ts
      provides: Shared helpers for ordering, legacy game-1 normalization, and one-game effective winner access.
      min_lines: 40
    - path: badminton-v2/src/hooks/useAdminActions.ts
      provides: Admin writer aligned to explicit `game_number: 1` for current one-game result submission.
      min_lines: 6
    - path: badminton-v2/src/components/CourtCard.tsx
      provides: Live-board writer aligned to explicit `game_number: 1`.
      min_lines: 6
    - path: badminton-v2/src/hooks/usePlayerStats.ts
      provides: Player stats read path using normalized result access instead of raw index assumptions.
      min_lines: 12
    - path: badminton-v2/src/hooks/usePlayerSchedule.ts
      provides: Completed-match winner derivation using normalized ordered results.
      min_lines: 12
    - path: badminton-v2/src/views/TodayView.tsx
      provides: Session leaderboard compatibility read path ready for later split expansion.
      min_lines: 12
    - path: badminton-v2/src/views/SessionPlayerDetailView.tsx
      provides: Session leaderboard compatibility read path ready for later split expansion.
      min_lines: 12
  key_links:
    - from: badminton-v2/src/lib/matchResults.ts
      to: badminton-v2/src/hooks/usePlayerStats.ts
      via: Shared one-game effective winner normalization.
    - from: badminton-v2/src/lib/matchResults.ts
      to: badminton-v2/src/views/TodayView.tsx
      via: Ordered `match_results` compatibility access.
    - from: badminton-v2/src/hooks/useAdminActions.ts
      to: badminton-v2/src/components/CourtCard.tsx
      via: Both current writers must emit explicit `game_number: 1`.
user_setup: []
---

<objective>
Normalize the app-side read and write paths around the new split-scoring contract so current one-game behavior stays stable and future split work can extend a shared foundation.
</objective>

<context>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/13-split-scoring-schema/13-CONTEXT.md
    - .planning/phases/13-split-scoring-schema/13-RESEARCH.md
    - .planning/phases/13-split-scoring-schema/13-01-PLAN.md
    - badminton-v2/src/types/database.ts
    - badminton-v2/src/hooks/useAdminActions.ts
    - badminton-v2/src/components/CourtCard.tsx
    - badminton-v2/src/hooks/usePlayerStats.ts
    - badminton-v2/src/hooks/usePlayerSchedule.ts
    - badminton-v2/src/views/TodayView.tsx
    - badminton-v2/src/views/SessionPlayerDetailView.tsx
  </read_first>
  <constraints>
    - Do not add split-result buttons, `2-0`, or `1-1` UX in this phase.
    - Keep leaderboard math equivalent for current one-game data.
    - Prefer one shared helper over repeated inline fallback logic.
    - Preserve current realtime and queue-advance behavior.
  </constraints>
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Create the shared match-results compatibility helper</name>
    <files>badminton-v2/src/lib/matchResults.ts, badminton-v2/src/__tests__/matchResults.test.ts</files>
    <action>
      Create a small shared helper module that exports the normalized result-row shape and the compatibility operations Phase 13 needs: stable ordering by `game_number`, legacy fallback semantics that treat one-game rows as game 1, and a helper that returns the effective winner for current one-game surfaces without forcing every caller to read `match_results[0]`. Keep the helper narrow and phase-scoped; it should prepare the contract for later split phases without implementing their full business logic yet. Expand the unit tests from Plan 01 to cover this finalized helper behavior.
    </action>
    <verify>
      <automated>npm run test:unit</automated>
    </verify>
    <done>
      D-07 and D-08 are satisfied through one reusable helper contract.
    </done>
  </task>

  <task type="auto">
    <name>Task 2: Align current result writers to the new contract</name>
    <files>badminton-v2/src/hooks/useAdminActions.ts, badminton-v2/src/components/CourtCard.tsx</files>
    <action>
      Update both existing result writers to insert `game_number: 1` explicitly when recording current one-game results. Preserve the existing finish behavior, queue advancement, and skip-recording branches exactly as they work today. Do not introduce split branching or new button flows; this task only makes the current writes explicit and future-safe.
    </action>
    <verify>
      <automated>npm run build</automated>
    </verify>
    <done>
      Current one-game completion paths write rows that already conform to the future split-ready schema.
    </done>
  </task>

  <task type="auto">
    <name>Task 3: Normalize current readers and record execution summary</name>
    <files>badminton-v2/src/hooks/usePlayerStats.ts, badminton-v2/src/hooks/usePlayerSchedule.ts, badminton-v2/src/views/TodayView.tsx, badminton-v2/src/views/SessionPlayerDetailView.tsx, .planning/phases/13-split-scoring-schema/13-02-SUMMARY.md</files>
    <action>
      Update the identified read paths that currently consume `match_results` so they select `game_number` and read through the new helper instead of assuming the first related row is authoritative. Keep current one-game output unchanged for player schedule, player stats, today leaderboard, and session leaderboard. Avoid Phase 15 scope creep: do not add multi-row aggregation semantics beyond what is necessary to preserve current behavior with ordered game-1 compatibility. Run `npm run lint`, `npm run test:unit`, and `npm run build`, then record files changed, commands, and any follow-up notes in `13-02-SUMMARY.md`.
    </action>
    <verify>
      <automated>npm run lint</automated>
      <automated>npm run test:unit</automated>
      <automated>npm run build</automated>
    </verify>
    <done>
      Existing completed one-game matches still read correctly everywhere touched in Phase 13, and the codebase no longer depends on unordered raw result arrays for those paths.
    </done>
  </task>
</tasks>

<verification>
  <automated>
    cd badminton-v2
    npm run lint
    npm run test:unit
    npm run build
  </automated>
  <manual>
    Confirm no user-facing split-result UI appears yet and that current one-game completion/leaderboard behavior is unchanged in code intent.
  </manual>
</verification>

<success_criteria>
- A shared helper defines the normalized result contract for current and future phases.
- Admin and live-board result writers explicitly insert `game_number: 1` for current one-game completions.
- Current one-game readers use ordered normalized result access instead of raw first-element assumptions.
- Existing completed one-game matches still read and count once across the touched surfaces.
- `npm run lint`, `npm run test:unit`, and `npm run build` pass, or any blocker is documented clearly.
</success_criteria>

<source_audit>
  <goal status="covered">Existing completed one-game matches still read as one-game results while the app adopts the split-ready contract.</goal>
  <requirements>
    <requirement id="FMT-01" status="covered">Readers can tolerate the new session flag contract without changing current behavior.</requirement>
    <requirement id="RES-03" status="covered">Writers and readers adopt the game-level result shape.</requirement>
    <requirement id="COMP-01" status="covered">Compatibility helper plus normalized readers preserve existing one-game semantics.</requirement>
  </requirements>
  <context_decisions>
    <decision id="D-07" status="covered">Task 1 introduces shared compatibility helpers now.</decision>
    <decision id="D-08" status="covered">Tasks 1 and 3 centralize the legacy game-1 rule.</decision>
  </context_decisions>
  <research status="covered">Uses the reader/writer inventory and anti-scope-creep guidance from `13-RESEARCH.md`.</research>
</source_audit>

<threat_model>
  <threat stride="integrity" disposition="mitigate">
    Ordered helper-based reads prevent future multi-row matches from being interpreted via arbitrary array order.
  </threat>
  <threat stride="tampering" disposition="mitigate">
    Explicit `game_number: 1` writes keep the current writers aligned with the DB contract.
  </threat>
  <threat stride="availability" disposition="mitigate">
    The phase preserves current UX and queue flow, reducing rollout risk while adopting the new contract.
  </threat>
</threat_model>

<output>
  <files_created>
    - badminton-v2/src/lib/matchResults.ts
    - .planning/phases/13-split-scoring-schema/13-02-SUMMARY.md
  </files_created>
  <files_modified>
    - badminton-v2/src/__tests__/matchResults.test.ts
    - badminton-v2/src/hooks/useAdminActions.ts
    - badminton-v2/src/components/CourtCard.tsx
    - badminton-v2/src/hooks/usePlayerStats.ts
    - badminton-v2/src/hooks/usePlayerSchedule.ts
    - badminton-v2/src/views/TodayView.tsx
    - badminton-v2/src/views/SessionPlayerDetailView.tsx
  </files_modified>
</output>
