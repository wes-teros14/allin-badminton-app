---
phase: 13-split-scoring-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql
  - badminton-v2/src/types/database.ts
  - badminton-v2/src/__tests__/matchResults.test.ts
  - .planning/phases/13-split-scoring-schema/13-01-SUMMARY.md
autonomous: true
requirements:
  - FMT-01
  - RES-03
  - RES-04
  - COMP-01
must_haves:
  truths:
    - Sessions expose a `split_match_scoring` boolean that defaults to false and preserves current one-game behavior.
    - Match results expose a non-null `game_number` that defaults to 1 for legacy and current one-game rows.
    - The database rejects duplicate `(match_id, game_number)` rows.
    - Existing data remains valid without null-handling or destructive rewrites.
  artifacts:
    - path: badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql
      provides: Additive schema migration for split scoring contract, uniqueness migration, and legacy backfill/default handling.
      min_lines: 45
    - path: badminton-v2/src/types/database.ts
      provides: Updated generated-style TypeScript contract for `sessions.split_match_scoring` and `match_results.game_number`.
      min_lines: 20
    - path: badminton-v2/src/__tests__/matchResults.test.ts
      provides: Unit tests for the normalized one-game/game-number compatibility rules used by later plans.
      min_lines: 40
  key_links:
    - from: badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql
      to: badminton-v2/src/types/database.ts
      via: SQL schema additions must be reflected in app-side table row/insert/update types.
    - from: badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql
      to: badminton-v2/src/__tests__/matchResults.test.ts
      via: Tests codify the legacy rule that current rows read as game 1.
user_setup:
  - Apply the migration through the Supabase Dashboard SQL Editor if local CLI remains blocked.
---

<objective>
Land the foundational split-scoring database and type contract so the app can represent session-level split mode and multiple game results per match without changing current one-game behavior.
</objective>

<context>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/13-split-scoring-schema/13-CONTEXT.md
    - .planning/phases/13-split-scoring-schema/13-RESEARCH.md
    - badminton-v2/supabase/migrations/002_create_sessions.sql
    - badminton-v2/supabase/migrations/007_match_results_and_court.sql
    - badminton-v2/supabase/migrations/013_player_stats_tables.sql
    - badminton-v2/src/types/database.ts
  </read_first>
  <constraints>
    - Work from `badminton-v2/` for npm commands.
    - Do not introduce nullable `game_number`.
    - Do not preserve a one-row-per-match uniqueness rule that blocks split matches.
    - Do not implement split-result UI or stats aggregation changes in this plan.
    - Preserve existing RLS and current one-game inserts/reads.
  </constraints>
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Add the split-scoring schema migration</name>
    <files>badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql</files>
    <action>
      Create a new migration that adds `split_match_scoring boolean not null default false` to `public.sessions` and `game_number integer not null default 1 check (game_number >= 1)` to `public.match_results`. Ensure existing rows are normalized to game 1 as part of the migration contract. Inspect the current `match_results` structure and remove or replace any unique rule that enforces one row per match, then add the authoritative uniqueness rule for `(match_id, game_number)`. Keep all current `match_results` policies and grants intact. Add concise comments for Dashboard SQL Editor application if the repo still relies on manual Supabase execution. Do not change trigger semantics in `013_player_stats_tables.sql`; Phase 13 is schema-readying, not stats redesign.
    </action>
    <verify>
      <manual>Review the SQL to confirm it is additive, preserves existing rows, and cannot allow duplicate game numbers per match.</manual>
    </verify>
    <done>
      FMT-01, RES-03, and RES-04 are established at the database layer, and legacy one-game rows remain valid as game 1.
    </done>
  </task>

  <task type="auto">
    <name>Task 2: Update the TypeScript database contract</name>
    <files>badminton-v2/src/types/database.ts</files>
    <action>
      Update the generated-style `Database` type definitions so `public.sessions.Row/Insert/Update` include `split_match_scoring`, and `public.match_results.Row/Insert/Update` include `game_number`. Keep the file shape consistent with the existing manual/generated format already in the repo. Make sure the additions do not introduce `never` errors in consuming code and preserve the existing relation metadata.
    </action>
    <verify>
      <automated>npm run build</automated>
    </verify>
    <done>
      The app can type-check queries and writes against the new schema fields without contract drift.
    </done>
  </task>

  <task type="auto">
    <name>Task 3: Define compatibility tests and execution summary</name>
    <files>badminton-v2/src/__tests__/matchResults.test.ts, .planning/phases/13-split-scoring-schema/13-01-SUMMARY.md</files>
    <action>
      Add unit tests that lock in the Phase 13 compatibility rules the next plan will rely on: a missing split-mode flag behaves like false, a single legacy result row is game 1, result rows can be sorted deterministically by `game_number`, and one-game result interpretation still produces the existing winner semantics. Run `npm run test:unit` and `npm run build`. Record commands run, migration application limitations, files changed, and any SQL execution blockers in `13-01-SUMMARY.md`. If the tests depend on a new helper module introduced in Plan 02, stub only the minimal exported contract needed and keep the rest of the implementation for Plan 02.
    </action>
    <verify>
      <automated>npm run test:unit</automated>
      <automated>npm run build</automated>
    </verify>
    <done>
      COMP-01 is captured as an executable test contract and the foundational schema/type work is verified or clearly blocked by environment limits.
    </done>
  </task>
</tasks>

<verification>
  <automated>
    cd badminton-v2
    npm run test:unit
    npm run build
  </automated>
  <manual>
    Confirm the migration removes any one-row-per-match blocker and replaces it with `(match_id, game_number)` uniqueness only.
  </manual>
</verification>

<success_criteria>
- `sessions` can store `split_match_scoring` with a default preserving current behavior.
- `match_results` can store multiple rows per match via `game_number`, with existing rows remaining valid as game 1.
- Duplicate `match_results` rows for the same match and game number are rejected by the database contract.
- `src/types/database.ts` reflects the new fields without introducing type errors.
- Unit/build verification passes, or any environment-only migration blocker is documented explicitly.
</success_criteria>

<source_audit>
  <goal status="covered">Database and TypeScript types can represent session-level split scoring and multiple game results per scheduled match.</goal>
  <requirements>
    <requirement id="FMT-01" status="covered">Tasks 1 and 2 add and type the session-level split-scoring flag.</requirement>
    <requirement id="RES-03" status="covered">Task 1 adds `game_number`; Task 2 types it.</requirement>
    <requirement id="RES-04" status="covered">Task 1 enforces composite uniqueness.</requirement>
    <requirement id="COMP-01" status="covered">Task 3 codifies legacy game-1 compatibility.</requirement>
  </requirements>
  <context_decisions>
    <decision id="D-01" status="covered">Task 1 adds `game_number integer not null default 1`.</decision>
    <decision id="D-02" status="covered">Tasks 1 and 3 preserve no-null legacy reads.</decision>
    <decision id="D-03" status="covered">Task 1 enforces `(match_id, game_number)` uniqueness.</decision>
    <decision id="D-04" status="covered">Task 1 removes the one-row-per-match blocker.</decision>
    <decision id="D-05" status="covered">Task 1 names the session field `split_match_scoring`.</decision>
    <decision id="D-06" status="covered">Task 1 defaults the session field to current behavior.</decision>
  </context_decisions>
  <research status="covered">Uses the additive-schema and shared-contract guidance from `13-RESEARCH.md`.</research>
</source_audit>

<threat_model>
  <threat stride="tampering" disposition="mitigate">
    Duplicate result inserts are prevented by database uniqueness rather than caller discipline.
  </threat>
  <threat stride="integrity" disposition="mitigate">
    Legacy rows are normalized to `game_number = 1`, so app reads do not branch on null.
  </threat>
  <threat stride="availability" disposition="mitigate">
    The migration is additive and preserves existing one-game behavior, minimizing rollout risk.
  </threat>
</threat_model>

<output>
  <files_created>
    - badminton-v2/supabase/migrations/063_add_split_scoring_schema.sql
    - badminton-v2/src/__tests__/matchResults.test.ts
    - .planning/phases/13-split-scoring-schema/13-01-SUMMARY.md
  </files_created>
  <files_modified>
    - badminton-v2/src/types/database.ts
  </files_modified>
</output>
