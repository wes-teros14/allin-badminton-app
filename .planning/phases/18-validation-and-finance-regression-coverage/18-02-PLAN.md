---
phase: 18-validation-and-finance-regression-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - badminton-v2/src/views/FinanceDetailView.tsx
  - badminton-v2/src/components/ManualAllocationEditor.tsx
  - badminton-v2/src/components/ManualBatchPickerDialog.tsx
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - COMP-01
must_haves:
  truths:
    - "Manual allocation validation shows visible inline feedback while the QM edits rows"
    - "Invalid manual allocations cannot be saved from the finance page even if duplicate-prevention UI is bypassed"
    - "Row-level feedback appears close to the affected manual row inputs rather than only as a generic save toast"
    - "Duplicate batch selection remains prevented in the picker and is also defended at save time"
    - "Saved auto and manual allocations continue to display correct total shuttles, shuttle cost, and batch allocation details after save"
  artifacts:
    - path: "badminton-v2/src/views/FinanceDetailView.tsx"
      provides: "manual validation-aware finance-page flow and save gating"
      contains: "Save Allocation"
    - path: "badminton-v2/src/components/ManualAllocationEditor.tsx"
      provides: "row-level inline validation feedback for manual allocation edits"
      contains: "shuttles"
---

<objective>
Integrate Phase 18 validation into the real finance page so the QM sees inline feedback during editing and cannot save invalid manual allocations.

Purpose: The hook-level validator should become visible and actionable in the manual editor itself. Phase 18 is not complete if invalid rows only fail after save or if the finance page can still clear or corrupt usage through an invalid manual submission path.
Output: `FinanceDetailView` and `ManualAllocationEditor` render inline validation state, keep duplicate-prevention UX intact, and hard-block invalid manual saves while preserving correct finance totals and allocation display.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-validation-and-finance-regression-coverage/18-CONTEXT.md
@.planning/phases/18-validation-and-finance-regression-coverage/18-RESEARCH.md
@.planning/phases/18-validation-and-finance-regression-coverage/18-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface manual validation state inline in the finance editor</name>
  <files>
    badminton-v2/src/views/FinanceDetailView.tsx,
    badminton-v2/src/components/ManualAllocationEditor.tsx
  </files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/components/ManualAllocationEditor.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Integrate live manual validation into the current editor flow.

Recommended behavior:
- derive validation from the current `manualRows` draft and the finance hook's available-batch stock contract
- show top-level feedback when no batches are selected
- show row-level feedback next to count inputs for duplicate, invalid-count, or over-stock states
- keep feedback visible while the QM edits rather than waiting until submit

Follow the repo's existing lightweight style: inline errors first, toasts only as secondary save feedback.
  </action>
  <done>
    - The QM sees live validation during editing
    - Row-specific issues are visible close to the edited input
  </done>
</task>

<task type="auto">
  <name>Task 2: Hard-block invalid manual saves while preserving finance compatibility displays</name>
  <files>
    badminton-v2/src/views/FinanceDetailView.tsx,
    badminton-v2/src/components/ManualBatchPickerDialog.tsx
  </files>
  <read_first>
    - badminton-v2/src/views/FinanceDetailView.tsx
    - badminton-v2/src/components/ManualBatchPickerDialog.tsx
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Wire the validated editor into the real save flow.

Recommended direction:
- disable or otherwise hard-block `Save Allocation` while invalid manual state remains
- preserve the existing picker duplicate-prevention behavior and align any helper copy with the new validation rules
- keep auto mode untouched
- make sure saved auto and manual allocations still render the correct total logged shuttles, shuttle cost, and batch allocation details after refetch

Do not change the underlying auto allocation algorithm. This plan hardens the manual path and preserves compatibility.
  </action>
  <done>
    - Invalid manual saves cannot succeed from the finance page
    - Auto/manual saved displays still read correctly after save
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run build` exits 0
- `npm.cmd run lint` exits 0
- `Select-String -Path badminton-v2/src/views/FinanceDetailView.tsx -Pattern "Save Allocation|manual|error"` returns manual validation-aware save flow lines
- `Select-String -Path badminton-v2/src/components/ManualAllocationEditor.tsx -Pattern "text-destructive|error|invalid"` returns inline validation rendering lines
</verification>

<success_criteria>
- Inline validation is visible while editing manual rows
- Invalid manual saves are blocked from the real finance page
- Saved auto/manual finance displays remain correct
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-and-finance-regression-coverage/18-02-SUMMARY.md`
</output>
