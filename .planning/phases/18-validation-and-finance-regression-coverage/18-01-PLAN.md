---
phase: 18-validation-and-finance-regression-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - badminton-v2/src/hooks/useSessionFinance.ts
  - badminton-v2/src/__tests__/useSessionFinance.test.ts
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - COMP-01
must_haves:
  truths:
    - "Manual shuttle allocation validation is enforced in the finance hook's save seam, not only in the UI"
    - "Manual saves are rejected when no batches are selected, when a batch appears more than once, or when any row count is not a whole number greater than 0"
    - "Manual saves are rejected when any row count exceeds the batch's currently available stock, using the existing reopen-safe excluded-session stock baseline"
    - "Automatic allocation behavior remains unchanged while manual validation logic is added"
    - "Unit coverage protects invalid manual cases and auto/manual finance compatibility helpers"
  artifacts:
    - path: "badminton-v2/src/hooks/useSessionFinance.ts"
      provides: "manual validation helpers and authoritative save-time enforcement"
      contains: "manual"
    - path: "badminton-v2/src/__tests__/useSessionFinance.test.ts"
      provides: "manual validation and finance regression helper coverage"
      contains: "manual"
---

<objective>
Add the validation foundation for manual shuttle allocation at the hook layer, with helper coverage that protects both new manual rules and existing automatic finance behavior.

Purpose: Phase 18 should fail invalid manual saves at the authoritative persistence seam before any UI refinements land. This keeps later finance-page work honest and prevents malformed rows from silently reaching `shuttle_usage`.
Output: `useSessionFinance` exposes manual validation-safe save behavior and the Vitest suite locks the invalid-case matrix plus compatibility-sensitive helper behavior.
</objective>

<execution_context>
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/workflows/execute-plan.md
@C:/1Wes/all-in-badminton-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-validation-and-finance-regression-coverage/18-CONTEXT.md
@.planning/phases/18-validation-and-finance-regression-coverage/18-RESEARCH.md
@.planning/phases/17-manual-batch-allocation-ui/17-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured manual allocation validation to useSessionFinance</name>
  <files>
    badminton-v2/src/hooks/useSessionFinance.ts,
    badminton-v2/src/__tests__/useSessionFinance.test.ts
  </files>
  <read_first>
    - badminton-v2/src/hooks/useSessionFinance.ts
    - badminton-v2/src/__tests__/useSessionFinance.test.ts
    - .planning/phases/18-validation-and-finance-regression-coverage/18-CONTEXT.md
  </read_first>
  <action>
Add a pure helper or helper set that validates manual rows before `buildUsageRowsForSave` returns insertable `shuttle_usage` records.

Recommended validation scope:
- reject empty manual allocations
- reject duplicate `batchId` rows even if the picker normally prevents them
- reject any row count that is not an integer greater than 0
- reject any row count above the batch's currently available stock
- shape validation output so later UI work can identify both top-level and row-level problems

Base stock checks on the existing `batchesForAllocation` / `availableManualBatches` contract so reopened sessions continue to exclude their own saved usage from the stock baseline.
  </action>
  <done>
    - Manual validation is centralized in the hook
    - Invalid manual saves fail before any database writes occur
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand helper coverage for invalid manual cases and compatibility-safe finance behavior</name>
  <files>badminton-v2/src/__tests__/useSessionFinance.test.ts</files>
  <read_first>
    - badminton-v2/src/__tests__/useSessionFinance.test.ts
    - badminton-v2/src/hooks/useSessionFinance.ts
  </read_first>
  <action>
Extend the unit suite around the new validation and regression-sensitive helpers.

Cover at least:
- empty manual allocation is rejected
- duplicate batch ids are rejected
- zero, negative, fractional, or otherwise invalid counts are rejected
- over-stock rows are rejected against the hook's available-stock contract
- a reopen/edit scenario still validates when the session's own prior usage is excluded
- current auto allocation helper behavior still produces the same cheapest-first results and error shape

Keep the suite pure-helper focused; do not introduce component rendering or jsdom infrastructure.
  </action>
  <done>
    - The invalid-case matrix is protected in Vitest
    - Auto/manual helper compatibility is locked by tests
  </done>
</task>

</tasks>

<verification>
- `npm.cmd run test:unit` passes
- `npm.cmd run build` exits 0
- `Select-String -Path badminton-v2/src/hooks/useSessionFinance.ts -Pattern "duplicate|stock|manual"` returns manual validation logic
- `Select-String -Path badminton-v2/src/__tests__/useSessionFinance.test.ts -Pattern "duplicate|empty|stock|manual"` returns validation coverage lines
</verification>

<success_criteria>
- Invalid manual allocations are blocked at save time
- Reopen-safe stock validation uses the existing excluded-session baseline
- Automatic allocation behavior remains unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-and-finance-regression-coverage/18-01-SUMMARY.md`
</output>
